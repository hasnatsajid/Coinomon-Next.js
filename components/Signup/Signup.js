import Modal from '../UI/Modal/Modal';

import './Signup.module.scss';

const Signup = ({ onBackdropClick }) => {
  return <Modal onBackdropClick={onBackdropClick}>signup</Modal>;
};

export default Signup;
