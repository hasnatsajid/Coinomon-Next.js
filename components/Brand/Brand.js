import styles from './Brand.module.scss';

const Brand = () => {
  return <h1 className={styles.brand}>{`Best learning platform for Web3.0, Blockchain and Cryptocurrency `}</h1>;
};

export default Brand;
