import Modal from '../UI/Modal/Modal';
import styles from './Login.module.scss';

const Login = ({ onBackdropClick }) => {
  return <Modal onBackdropClick={onBackdropClick}>hello</Modal>;
};

export default Login;
